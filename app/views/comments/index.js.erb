$("#comments_area").html("<%= j(render 'comments/index', { comments: @comment.post.comments.order(created_at: :asc), post: @comment.post }) %>")
$("textarea").val('')
// $("html,body").animate({scrollTop:$('#comments_area').offset().top});
