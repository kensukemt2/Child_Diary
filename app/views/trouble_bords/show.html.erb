<%= @bord.each do |bord| %>
  <%= bord.title %>
  <%= bord.conten %>
<% end %>
<% @comments.each do |comment| %>
  <%= comment.comment %>
  <%= comment.user.name %>
<% end %>
<%= form_with(model: @comment, local: true) do |comment| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.submit "投稿する" %>
<% end %>
