  <%= @bord.title %>
  <%= @bord.content %>
  <%= @bord.user.name %>

<% @comments.each do |comment| %>
  <%= comment.comment %>
  <%= comment.user.name %>
<% end %>
<%= form_with(model: [@bord, @newcomment], local: true) do |f| %>
  <%= f.label :comment %>
  <%= f.text_field :comment %>
  <%= f.submit "コメントする" %>
<% end %>
<%= link_to "掲示板一覧へ", trouble_bords_path %>
